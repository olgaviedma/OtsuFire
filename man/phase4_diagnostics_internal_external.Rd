% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_histograms_tests.R
\name{phase4_diagnostics_internal_external}
\alias{phase4_diagnostics_internal_external}
\title{Phase-4 diagnostics for internal vs reference burned-area polygons}
\usage{
phase4_diagnostics_internal_external(
  internal_sf,
  ref_sf,
  rbr_rast = NULL,
  out_dir,
  value_cols = c("median_rbr"),
  do_histograms = TRUE,
  do_tests = TRUE,
  hist_dir = file.path(out_dir, "phase4_histograms"),
  tests_dir = file.path(out_dir, "phase4_tests"),
  buffer_m = 0,
  chunk_size = 500,
  internal_flag_col = NULL,
  internal_keep_value = "keep",
  internal_review_value = "review",
  ref_flag_col = "flag_ref",
  ref_keep_value = "keep_ref",
  ref_review_value = "review_ref",
  use_internal_effis_flag = TRUE,
  internal_effis_col = "flag_internal_effis",
  keep_common_value = "keep_common",
  review_int_no_effis_value = "review_int_no_effis",
  make_extra_no_effis = FALSE,
  compare_class3 = TRUE,
  class_col_internal = "class3_id",
  class_col_ref = "class3_id",
  class_levels_internal = NULL,
  class_levels_ref = NULL,
  hist_class_pairwise = TRUE,
  hist_class_pairwise_mode = c("cross_dataset", "all_pairs"),
  hist_class_pairwise_min_n = 20,
  hist_class_pairwise_max_pairs = Inf,
  hist_class_pairwise_include_within_dataset = FALSE,
  test_class_pairwise = c("all_pairs_internal_vs_ref", "same_class_internal_vs_ref",
    "within_internal", "within_ref", "all"),
  drop_na_class = TRUE,
  min_n_hist = 5,
  min_n_test = 10,
  do_ks = TRUE,
  save_test_outputs = FALSE,
  test_prefix = "phase4",
  return_sf = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{internal_sf}{`sf` polygon layer (internal), or a list with `scored_sf`.}

\item{ref_sf}{`sf` polygon layer (reference), or a list with `scored_sf`.}

\item{rbr_rast}{Optional `terra::SpatRaster`. Required only if `"median_rbr"` must be computed.}

\item{out_dir}{Output folder (base). Subfolders are created when needed.}

\item{value_cols}{Character vector of numeric columns to analyze.}

\item{do_histograms}{Logical. If TRUE, histogram PNGs are generated.}

\item{do_tests}{Logical. If TRUE, two-sample tests are computed.}

\item{hist_dir}{Folder for histogram outputs. Default: `file.path(out_dir, "phase4_histograms")`.}

\item{tests_dir}{Folder for test outputs. Default: `file.path(out_dir, "phase4_tests")`.}

\item{buffer_m}{Numeric. Buffer (meters) applied to `ref_keep` only for the geometry
fallback that derives `keep_common` from `internal_keep` intersecting `ref_keep`.}

\item{chunk_size}{Integer. Chunk size passed to `add_polygon_median_rbr()`.}

\item{internal_flag_col}{Character. Flag column used to define keep/review groups in the
internal dataset. If NULL, it is auto-detected (`flag_internal`, then `flag_rbr`).}

\item{internal_keep_value, internal_review_value}{Character. Keep/review labels in internal flags.}

\item{ref_flag_col}{Character. Flag column in the reference dataset.}

\item{ref_keep_value, ref_review_value}{Character. Keep/review labels in reference flags.}

\item{use_internal_effis_flag}{Logical. If TRUE and `internal_effis_col` exists,
`keep_common` prefers `keep_common_value` from that column.}

\item{internal_effis_col}{Character. Column holding Phase-1b / cross-flag labels.}

\item{keep_common_value}{Character. Label for common keep polygons.}

\item{review_int_no_effis_value}{Character. Label for "review internal, no reference".}

\item{make_extra_no_effis}{Logical. If TRUE, extra histogram overlays are generated using
`review_int_no_effis_value` when present.}

\item{compare_class3}{Logical. If TRUE, run class-based comparisons.}

\item{class_col_internal, class_col_ref}{Character. Class columns in internal/ref.}

\item{class_levels_internal, class_levels_ref}{Optional character vectors restricting class levels (NULL = all).}

\item{hist_class_pairwise}{Logical. If TRUE, generate pairwise class histograms.}

\item{hist_class_pairwise_mode}{One of `"cross_dataset"` or `"all_pairs"`.}

\item{hist_class_pairwise_min_n}{Integer. Minimum finite values per group for pairwise class histograms.}

\item{hist_class_pairwise_max_pairs}{Integer. Safety cap for number of pairwise plots.}

\item{hist_class_pairwise_include_within_dataset}{Logical. Only used when mode is `"all_pairs"`.}

\item{test_class_pairwise}{One of: `"all_pairs_internal_vs_ref"`, `"same_class_internal_vs_ref"`,
`"within_internal"`, `"within_ref"`, or `"all"`.}

\item{drop_na_class}{Logical. If TRUE, drops NA/empty class labels before testing.}

\item{min_n_hist}{Integer. Minimum finite values per group to draw a histogram overlay.}

\item{min_n_test}{Integer. Minimum finite values per group to run tests.}

\item{do_ks}{Logical. If TRUE, compute KS p-values (in addition to Wilcoxon).}

\item{save_test_outputs}{Logical. If TRUE, writes CSV/TXT summaries to `tests_dir`.}

\item{test_prefix}{Character. Prefix for saved test filenames.}

\item{return_sf}{Logical. If TRUE, returns updated `internal_sf` and `ref_sf` (useful when `median_rbr` was computed).}

\item{verbose}{Logical.}
}
\value{
A list with:
\itemize{
  \item `hist_paths`: named list by `value_col` (each an output-path list), or empty.
  \item `tests`: named list by `value_col` (each a `test_phase4_distributions2()` result), or empty.
  \item `out_dirs`: list with `hist_dir` and `tests_dir`.
  \item `internal_sf` / `ref_sf`: only when `return_sf=TRUE`.
}
}
\description{
Runs a combined diagnostic workflow to compare numeric distributions between an
internal polygon layer (your map) and a reference polygon layer (e.g., EFFIS):
\itemize{
  \item Overlaid percentage histograms for key group comparisons (optional).
  \item Two-sample distribution tests (Wilcoxon; optional KS; Cliff's delta),
        including optional class-stratified comparisons (optional).
}
}
\details{
The function accepts `sf` objects (polygons) or list outputs that contain a
`scored_sf` element (it will automatically extract it).

If `value_cols` includes `"median_rbr"` and that column is missing or has no
finite values in either dataset, the function computes it once using
`add_polygon_median_rbr()` (requires `rbr_rast`).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_chain_cleaning.R
\name{apply_chain_cleaning}
\alias{apply_chain_cleaning}
\alias{apply_chain}
\title{Remove narrow connections from refined candidate burned patches}
\usage{
apply_chain_cleaning(
  refine_path,
  out_dir = NULL,
  year = NULL,
  dechain_distance_m = 180,
  pixel_size_m = 90,
  inflation_thr = 22.135,
  min_area_ha = NULL,
  marker = c("connected", "seeds"),
  keep_noseed_fragments = TRUE,
  change_index = NULL,
  vegetation_map = NULL,
  seed_threshold_by_vegetation = NULL,
  overwrite = FALSE,
  verbose = TRUE
)

apply_chain(
  refine_path,
  out_dir = NULL,
  year = NULL,
  dechain_distance_m = 180,
  pixel_size_m = 90,
  inflation_thr = 22.135,
  min_area_ha = NULL,
  marker = c("connected", "seeds"),
  keep_noseed_fragments = TRUE,
  change_index = NULL,
  vegetation_map = NULL,
  seed_threshold_by_vegetation = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{refine_path}{Character scalar. Path to the refined candidate
GeoPackage, typically \verb{02_REFINE/BA_<year>_REFINE_MERGED_*.gpkg}. Use the
\code{refined_patches_path} returned by \code{\link[=detect_burned_patches]{detect_burned_patches()}}.}

\item{out_dir}{Character scalar or \code{NULL}. Run directory under which
\verb{CHAIN/dechain_<N>m/} is created. When \code{NULL}, uses
\code{dirname(dirname(refine_path))}, assuming the input is inside a
\verb{02_REFINE} folder.}

\item{year}{Integer scalar or \code{NULL}. Target year. When \code{NULL}, the year is
extracted from the input filename.}

\item{dechain_distance_m}{Numeric scalar. Morphological opening radius in
metres, converted to pixels using \code{pixel_size_m}. Larger values produce
stronger cleaning. Default: \code{180}.}

\item{pixel_size_m}{Numeric scalar. Grid-cell size in metres used to
rasterise selected candidates. Default: \code{90}.}

\item{inflation_thr}{Numeric scalar. Shape-complexity threshold used to
select candidates for cleaning. Candidates are selected when
\code{1 / sqrt(compact_pp)} exceeds this value. Default: \code{22.135}.}

\item{min_area_ha}{Numeric scalar or \code{NULL}. Optional minimum candidate
area, in hectares, for selection. When supplied, candidates must also
satisfy \code{area_ha >= min_area_ha}. Default: \code{NULL}, meaning no additional
area requirement.}

\item{marker}{Character scalar. Component-retention mode: \code{"connected"} or
\code{"seeds"}. Default: \code{"connected"}. See \strong{Component retention}
below.}

\item{keep_noseed_fragments}{Logical scalar. With \code{marker = "seeds"},
whether to retain fragments containing no seed pixels. Default: \code{TRUE}.}

\item{change_index}{Character scalar or \code{NULL}. Path to the RBR
change-index raster used to identify seeds when \code{marker = "seeds"}.}

\item{vegetation_map}{Character scalar or \code{NULL}. Path to the
CORINE-compatible vegetation-class raster used when \code{marker = "seeds"}.}

\item{seed_threshold_by_vegetation}{Named numeric vector or \code{NULL}.
Vegetation-specific seed thresholds matching those used during candidate
generation. Required when \code{marker = "seeds"}.}

\item{overwrite}{Logical scalar. Whether existing chain-cleaning outputs may
be replaced. The original refined layer is preserved. Default: \code{FALSE}.}

\item{verbose}{Logical scalar. Whether to display progress messages.
Default: \code{TRUE}.}
}
\value{
A named list containing output locations, processing settings, and
before-and-after summaries.\tabular{ll}{
   Field \tab Description \cr
   \code{output_path} \tab Path to the cleaned candidate layer. \cr
   \code{audit_path} \tab Path to the cleaning audit output. \cr
   \code{params_path} \tab Path to the saved cleaning parameters. \cr
   \code{n_candidates_before} \tab Number of candidates before cleaning. \cr
   \code{n_candidates_after} \tab Number of candidates after cleaning. \cr
   \code{n_chained_flagged} \tab Number of candidates flagged for cleaning. \cr
   \code{area_before_ha} \tab Candidate area before cleaning, in hectares. \cr
   \code{area_after_ha} \tab Candidate area after cleaning, in hectares. \cr
   \code{area_removed_ha} \tab Reported area removed by cleaning, in hectares. \cr
   \code{opening_px} \tab Opening radius used, in pixels. \cr
   \code{dechain_distance_m} \tab Cleaning-distance setting, in metres. \cr
}


Candidate counts may increase when a polygon is split into several
components.
}
\description{
Optionally clean narrow connections between parts of refined candidate
burned patches. These connections can arise during region growing and join
otherwise separate patches.

The function identifies candidates with unusually complex shapes, applies
morphological opening to those candidates, and writes a separate layer for
subsequent scoring. Candidates not selected for cleaning are retained
without rasterisation or morphological processing.

Run this function after \code{\link[=detect_burned_patches]{detect_burned_patches()}} and pass the cleaned
output to \code{\link[=score_burned_patches]{score_burned_patches()}}. The original refined candidate layer
is preserved.

\code{apply_chain()} is an alias for \code{apply_chain_cleaning()}.
}
\section{Position in the workflow}{

Chain cleaning is an optional step between refinement and scoring:
\enumerate{
\item Generate refined candidates with \code{\link[=detect_burned_patches]{detect_burned_patches()}}.
\item Clean selected candidates with \code{apply_chain_cleaning()}.
\item Score the cleaned layer with \code{\link[=score_burned_patches]{score_burned_patches()}}.
}

Cleaning is applied after refinement so that the refinement merging step
does not reconnect separated patches. Existing detection outputs can be
reused without repeating region growing or refinement.

The standard Otsu-guided segmentation pipeline does not automatically run this
optional step.
}

\section{Candidate selection}{

Candidates are selected using a shape-complexity measure derived from
Polsby-Popper compactness:
\preformatted{
compact_pp = 4 * pi * A / P^2
inflation  = 1 / sqrt(compact_pp)
}
Here, \code{A} is polygon area and \code{P} is perimeter, expressed in consistent
units.

A candidate is selected when \code{inflation > inflation_thr}. If \code{min_area_ha}
is supplied, the candidate must also satisfy \code{area_ha >= min_area_ha}.

Shape complexity identifies candidates for inspection and cleaning; it
does not establish that a narrow connection is an artefact.

\code{min_area_ha} controls which input candidates are processed. It is not a
minimum-area filter for the resulting fragments.
}

\section{Morphological cleaning}{

Selected candidates are:
\enumerate{
\item rasterised at \code{pixel_size_m};
\item processed by morphological opening, consisting of erosion followed by
dilation with a disk-shaped structuring element;
\item separated into connected components;
\item converted back to polygons.
}

Opening can remove narrow connections and small protrusions. It can also
remove small components or alter boundaries within selected candidates.

Candidates not selected for cleaning are retained without rasterisation or
morphological processing.
}

\section{Distance and pixel size}{

The opening radius in pixels is calculated as
\code{opening_px = dechain_distance_m / pixel_size_m}.

For a grid-cell size of 90 m:\tabular{lll}{
   \code{dechain_distance_m} \tab Opening radius in pixels \tab Nominal opening diameter \cr
   90 \tab 1 \tab 180 m \cr
   180 \tab 2 \tab 360 m \cr
   270 \tab 3 \tab 540 m \cr
}


The opening diameter is not an exact cutoff for connection width. Results
depend on connection shape, orientation, and rasterisation.

If the distance-to-pixel ratio is not an integer, the function issues a
warning and rounds it using \code{round()}. An opening radius below one pixel
is rejected.
}

\section{Component retention}{
\tabular{ll}{
   Mode \tab Behaviour \cr
   \code{"connected"} \tab Retain every connected component remaining after opening. \cr
   \code{"seeds"} \tab Use seed support to assess the resulting fragments. Requires \code{change_index}, \code{vegetation_map}, and \code{seed_threshold_by_vegetation}. \cr
}


With \code{marker = "seeds"}:
\itemize{
\item \code{keep_noseed_fragments = TRUE} retains fragments without seed support;
\item \code{keep_noseed_fragments = FALSE} removes fragments without seed support.
}

If seed mode is requested without the required inputs, the function issues
a warning and falls back to \code{"connected"} mode.
}

\section{Output location}{

Cleaned outputs are written under \verb{<out_dir>/CHAIN/dechain_<N>m/}.

The original \code{REFINE_MERGED} input is preserved. To control the output
location explicitly, supply \code{out_dir}, particularly when the input is
outside the standard run-folder structure.
}

\section{Choosing cleaning settings}{

Larger opening radii can separate genuine parts of a fire or remove small
burned patches. Lowering \code{inflation_thr} selects more candidates for
processing.

Inspect the cleaned geometries and compare candidate counts and areas
before using the output for scoring. Area removed by cleaning should not
automatically be interpreted as corrected false-positive area.
}

\examples{
\dontrun{
# Configure an annual RBR workflow
config <- build_burned_mapping_config(
  change_index = "data/RBR_2022.tif",
  vegetation_map = "data/vegetation_classes.tif",
  burnable_mask = "data/burnable_mask.tif",
  hotspots = "data/hotspots_2022.gpkg",
  target_year = 2022L,
  output_dir = "results",
  run_name = "RBR_2022"
)

# Generate refined candidates
detection <- detect_burned_patches(
  config = config,
  write_outputs = TRUE
)

# Clean selected candidates using a two-pixel opening radius
cleaned <- apply_chain_cleaning(
  refine_path = detection$refined_patches_path,
  year = 2022L,
  dechain_distance_m = 180,
  pixel_size_m = 90
)

# Inspect changes in candidate counts and area
cleaned$n_chained_flagged
cleaned$n_candidates_before
cleaned$n_candidates_after
cleaned$area_removed_ha

# Load the cleaned layer for visual inspection
cleaned_patches <- sf::st_read(
  cleaned$output_path,
  quiet = TRUE
)
plot(sf::st_geometry(cleaned_patches))

# Compare a smaller opening radius using the same original input
cleaned_90 <- apply_chain_cleaning(
  refine_path = detection$refined_patches_path,
  year = 2022L,
  dechain_distance_m = 90,
  pixel_size_m = 90
)

# After inspection, score the selected cleaned output
scored <- score_burned_patches(
  burned_candidates = cleaned$output_path,
  config = config,
  write_outputs = TRUE
)

table(
  scored$internal_decisions$class_final,
  useNA = "ifany"
)
}

}
\seealso{
\code{\link[=build_burned_mapping_config]{build_burned_mapping_config()}}, \code{\link[=detect_burned_patches]{detect_burned_patches()}},
\code{\link[=score_burned_patches]{score_burned_patches()}}.
}

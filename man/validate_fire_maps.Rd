% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_validate_fire_maps.R
\name{validate_fire_maps}
\alias{validate_fire_maps}
\title{Validate fire detections against a reference dataset (pixel, polygon/area, and optional classwise metrics)}
\usage{
validate_fire_maps(
  input_shapefile,
  ref_shapefile,
  mask_shapefile,
  year_target,
  validation_dir,
  template_res = 100,
  min_detected_percent = 10,
  threshold_completely_detected = 90,
  min_area_reference_ha = NULL,
  force_reprocess_ref = FALSE,
  force_reprocess_pred = FALSE,
  metrics_type = c("all", "pixel", "area"),
  dissolve_ref_by = NULL,
  dissolve_input_by = NULL,
  burned_value = 1,
  pixel_domain = c("mask", "complete_cases"),
  pixel_na_as_zero = TRUE,
  strict_binary_check = FALSE,
  class_raster = NULL,
  class_band = 1,
  class_values = NULL,
  class_lut = NULL,
  class_domain = c("class", "complete_cases"),
  class_na_as_zero = TRUE,
  class_chunk_rows = 2048,
  class_min_ref_area_ha = 0,
  class_include_all_lut_ids = FALSE,
  class_shape = NULL,
  class_field = NULL,
  write_classwise_metrics = FALSE,
  write_classwise_errors = FALSE,
  input_layer = NULL,
  ref_layer = NULL,
  union_detections_for_area = TRUE,
  arcgis_wkt1 = TRUE,
  output_prefix = "VAL",
  output_tag = NULL,
  tag_as_subdir = TRUE,
  outputs_subdir = "VAL",
  errors_subdir = "ERR",
  write_error_vectors = TRUE,
  index_width = 2L,
  digits_metrics = 3,
  digits_area = 2,
  digits_res = 0,
  run_tag = NULL,
  ref_tag = NULL,
  verbose = TRUE,
  write_xlsx = FALSE,
  xlsx_path = NULL,
  xlsx_overwrite = TRUE,
  write_confusion_vectors = FALSE,
  write_confusion_pixel_raster = FALSE,
  confusion_subdir = "CONF",
  confusion_polys_layer = "confusion_polys",
  confusion_polys_suffix = "confusion_polys",
  confusion_pixels_suffix = "confusion_pixels",
  compute_det_overlap_stats = FALSE,
  ...
)
}
\arguments{
\item{input_shapefile}{Path(s) to detected polygons (vector), or an \code{sf} object.
If multiple paths are provided, outputs are indexed by \code{InputIndex}.}

\item{ref_shapefile}{Path to reference polygons, or an \code{sf} object.}

\item{mask_shapefile}{Path to AOI mask polygon (sf-readable).}

\item{year_target}{Integer. Target year to filter reference polygons if a year-like field exists
(first match among \code{year/Year/YEAR}).}

\item{validation_dir}{Character. Output base directory.}

\item{template_res}{Numeric. Grid resolution (meters) for pixel metrics and for rasterizing classwise errors.}

\item{min_detected_percent}{Numeric. Reference polygon considered detected if \code{Coverage_i >=} this (\%).}

\item{threshold_completely_detected}{Numeric. Reference polygon considered completely detected if \code{Coverage_i >=} this (\%).}

\item{min_area_reference_ha}{Numeric or NULL. Minimum reference polygon area to retain (ha). If NULL, no filter.}

\item{force_reprocess_ref}{Logical. If TRUE, delete cached reference products and rebuild them.}

\item{force_reprocess_pred}{Logical. If TRUE, rebuild prediction rasters (per input).}

\item{metrics_type}{One of \code{c("all","pixel","area")}. Controls which metric blocks to compute.}

\item{dissolve_ref_by}{Character or NULL. Optional dissolve field for reference polygons \strong{after clipping to the mask}.
Use this to recombine clipped fragments that share the same ID (e.g., multiple parts of the same fire perimeter).
Dissolving aggregates non-geometry attributes within each ID (see \strong{Dissolve behavior} section).
Set to NULL to skip dissolve and preserve clipped fragments as separate rows.}

\item{dissolve_input_by}{Character or NULL. Optional dissolve field for detection polygons \strong{after clipping to the mask}.
Use this to keep \strong{one feature per original polygon ID} when the mask clip fragments geometries.
Set to NULL to preserve clipped fragments (and avoid attribute aggregation).}

\item{burned_value}{Numeric/integer. Pixel value representing "burned" in rasterized prediction/reference (default 1).}

\item{pixel_domain}{One of \code{c("mask","complete_cases")}. Kept for call compatibility; current implementation
evaluates over the mask domain raster (\code{domain_mask}).}

\item{pixel_na_as_zero}{Logical. Kept for call compatibility; reference/pred are 0/1 inside mask and NA outside.}

\item{strict_binary_check}{Logical. If TRUE, stop if the prediction raster contains values other than 0/1 (excluding NA).}

\item{class_raster}{Optional categorical raster for class-wise metrics (path or \code{SpatRaster}).}

\item{class_band}{Integer. Band index if \code{class_raster} has multiple layers.}

\item{class_values}{Optional numeric vector. Subset of class codes to keep (others set NA).}

\item{class_lut}{Optional LUT (data.frame or CSV path). Must define \code{id} and \code{label} columns (or first two columns are used).}

\item{class_domain}{One of \code{c("class","complete_cases")}. Domain definition for class-wise metrics.}

\item{class_na_as_zero}{Logical. If TRUE, treat NA in pred/ref as 0 within the classwise domain.}

\item{class_chunk_rows}{Integer. Chunk size (rows) for class-wise raster streaming (memory/performance control).}

\item{class_min_ref_area_ha}{Numeric. Minimum reference area per class (ha) to report in classwise metrics.}

\item{class_include_all_lut_ids}{Logical. If TRUE (and LUT provided), include LUT ids even if absent in raster (filled as 0/NA as appropriate).}

\item{class_shape}{Deprecated/optional. Vector strata layer (kept for backward compatibility; currently not used).}

\item{class_field}{Deprecated/optional. Field in \code{class_shape} defining strata (currently not used).}

\item{write_classwise_metrics}{Logical. If TRUE, write class-wise metrics CSV(s).}

\item{write_classwise_errors}{Logical. If TRUE, compute omission/commission area by class (CSV outputs).}

\item{input_layer}{Optional layer name for GeoPackage detections (\code{input_shapefile}).
If NULL and input is a .gpkg, the function auto-picks a preferred layer when available.}

\item{ref_layer}{Optional layer name for GeoPackage reference (\code{ref_shapefile}).
If NULL and reference is a .gpkg, the function auto-picks a preferred layer when available.}

\item{union_detections_for_area}{Logical. If TRUE, union detections before area intersection (avoids overlap double-counting).}

\item{arcgis_wkt1}{Logical. If TRUE, write GeoPackage outputs with WKT1_ESRI CRS serialization (ArcGIS-friendly).}

\item{output_prefix}{Character. Short prefix used in filenames.}

\item{output_tag}{Optional character tag used in folder naming (if \code{tag_as_subdir=TRUE}).}

\item{tag_as_subdir}{Logical. If TRUE, write outputs into a subfolder \code{prefix_tag} under \code{outputs_subdir}.}

\item{outputs_subdir}{Character. Subfolder name under \code{validation_dir} for outputs.}

\item{errors_subdir}{Character. Subfolder name under outputs for error vectors (when enabled).}

\item{write_error_vectors}{Logical. If TRUE, write omission/commission error vectors (GeoPackage).}

\item{index_width}{Integer. Width for indexed filenames if multiple inputs are validated.}

\item{digits_metrics}{Integer. Decimal digits for ratio/percent metrics (e.g., Precision, Recall, F1, IoU, ErrorRate, area recall/precision).}

\item{digits_area}{Integer. Decimal digits for area fields in hectares.}

\item{digits_res}{Integer. Decimal digits for resolution-related fields (e.g., TemplateRes_m).}

\item{run_tag}{Optional character metadata written into output tables.}

\item{ref_tag}{Optional character metadata written into output tables.}

\item{verbose}{Logical. If TRUE, prints progress messages.}

\item{write_xlsx}{Logical. If TRUE, write a summary workbook (requires \pkg{openxlsx}).}

\item{xlsx_path}{Optional character. Explicit path for the workbook; if NULL, a default name is used in \code{validation_output_dir}.}

\item{xlsx_overwrite}{Logical. If TRUE, overwrite existing workbook at \code{xlsx_path}.}

\item{write_confusion_vectors}{Logical. If TRUE, write a GeoPackage with reference + detection polygons labeled TP/FP/FN.}

\item{write_confusion_pixel_raster}{Logical. If TRUE, write a pixel confusion GeoTIFF coded as 1=TN, 2=FN, 3=FP, 4=TP.}

\item{confusion_subdir}{Character. Subfolder name under \code{validation_output_dir} for confusion outputs.
If NULL/empty/NA, it defaults to \code{"CONF"}.}

\item{confusion_polys_layer}{Character. Layer name for the confusion polygon GeoPackage.}

\item{confusion_polys_suffix}{Character. Filename suffix for confusion polygons (used by the internal naming helper).}

\item{confusion_pixels_suffix}{Character. Filename suffix for confusion pixels raster (used by the internal naming helper).}

\item{compute_det_overlap_stats}{Logical. If TRUE, add overlap area and percent (vs reference union) to detection confusion polygons.}

\item{...}{Reserved for future use.}
}
\value{
A list with elements:
\itemize{
  \item \code{metrics}: \code{data.table} of global pixel metrics (one row per input) or NULL if \code{metrics_type="area"}.
  \item \code{polygon_summary}: \code{data.table} of polygon/area summaries (one row per input) or NULL if \code{metrics_type="pixel"}.
  \item \code{validation_output_dir}: output folder used.
  \item \code{errors_dir}: error vectors folder (created even if empty; depends on options).
  \item \code{xlsx_path}: workbook path (if written), otherwise NULL.
  \item \code{output_prefix}: sanitized prefix used in filenames.
  \item \code{output_tag}: sanitized tag used in folder naming (empty string if none).
  \item \code{confusion_polys_paths}: list of GeoPackage paths (per input; NULL entries if not written).
  \item \code{confusion_pixels_paths}: list of GeoTIFF paths (per input; NULL entries if not written).
}
}
\description{
Validates a set of detected (predicted) burned-area polygons against a reference dataset
within an AOI mask. The function rasterizes both reference and detections on a common
analysis grid (at \code{template_res}) and computes:
\itemize{
  \item Pixel/grid confusion-matrix metrics (TP/FP/FN/TN and derived scores)
  \item Polygon/area overlap summaries (coverage-based detection of reference polygons)
  \item Optional class-wise pixel metrics using a categorical raster (e.g., CORINE strata)
  \item Optional omission/commission summaries by class (via unmatched polygons)
  \item Optional confusion outputs: a pixel-coded confusion raster and/or polygon confusion vectors
}
}
\details{
Detailed behavior is organized in the sections below.
}
\section{Pixel (grid) validation (confusion matrix)}{

The AOI mask polygon is rasterized to an analysis grid at \code{template_res} meters.
Reference and detected polygons are rasterized on the same grid, producing two binary rasters.
Confusion-matrix counts are computed over the grid domain.

Main metrics reported:
\itemize{
  \item \strong{Precision} = TP / (TP + FP)
  \item \strong{Recall} = TP / (TP + FN)
  \item \strong{F1} = 2 * Precision * Recall / (Precision + Recall)
  \item \strong{IoU} = TP / (TP + FP + FN)
  \item \strong{ErrorRate} = (FP + FN) / (TP + FP + FN + TN)
  \item \strong{Commission} = 1 - Precision
  \item \strong{Omission} = 1 - Recall
}
}

\section{Polygon / area validation}{

Polygon-level detection is based on coverage of each reference polygon by detections:
\eqn{Coverage_i = 100 * Area(ref_i \cap det) / Area(ref_i)}{Coverage_i = 100*A(intersection)/A(ref)}.
A reference polygon is detected when \code{Coverage_i >= min_detected_percent}, and completely
detected when \code{Coverage_i >= threshold_completely_detected}.
}

\section{Dissolve behavior}{

Reference and detection polygons are clipped to the mask. Optional post-clip dissolve can be applied
with \code{dissolve_ref_by} and \code{dissolve_input_by} to recombine fragments by ID.
}

\section{Confusion outputs (optional)}{

If \code{write_confusion_pixel_raster=TRUE}, a pixel confusion raster is written:
\itemize{
  \item 1 = TN
  \item 2 = FN
  \item 3 = FP
  \item 4 = TP
}
If \code{write_confusion_vectors=TRUE}, a GeoPackage with TP/FP/FN polygon labels is written.
}

\section{Class-wise metrics (optional)}{

If \code{class_raster} is provided, confusion-matrix metrics are also computed by class.
Optional class-wise omission/commission summaries are written when
\code{write_classwise_errors=TRUE}.
}

\section{Caching and reproducibility}{

Reference polygons and rasterized reference masks are cached inside the output folder.
Use \code{force_reprocess_ref=TRUE} to force a rebuild.
}

\section{ArcGIS-friendly outputs}{

When \code{arcgis_wkt1=TRUE}, GeoPackages are written with WKT1_ESRI CRS serialization
and via a temporary-write/rename strategy to reduce lock issues on Windows/ArcGIS.
}

\section{Internal helper functions (defined inside the function)}{

\itemize{
  \item \code{.safe_slug}, \code{short_id}, \code{round_outputs}, \code{make_valid_sf}
  \item \code{transform_to_crs_safe}, \code{dissolve_by_field}, \code{safe_vect}, \code{read_sf_any}
  \item \code{sanitize_for_gpkg}, \code{write_gpkg_safe}, \code{out_file}
  \item \code{terra_freq_noNA}, \code{classwise_pixel_metrics}
}
}

\examples{
\dontrun{
# Single input (GPKG) + classwise CORINE + confusion outputs
res <- validate_fire_maps(
  input_shapefile = "D:/PROJECT/DET/internal_scored.gpkg",
  ref_shapefile   = "D:/PROJECT/REF/effis_ref.gpkg",
  mask_shapefile  = "D:/PROJECT/MASK/aoi_3035.shp",
  year_target     = 2025,
  validation_dir  = "D:/PROJECT/VALIDATION",
  input_layer     = "scored",
  ref_layer       = "polys",
  metrics_type    = "all",
  template_res    = 90,
  min_detected_percent = 10,
  threshold_completely_detected = 50,
  min_area_reference_ha = 1,
  force_reprocess_ref  = TRUE,
  force_reprocess_pred = TRUE,
  union_detections_for_area = TRUE,
  arcgis_wkt1 = TRUE,
  output_prefix = "VAL",
  output_tag    = "INT_2025_ALL",
  tag_as_subdir = TRUE,
  outputs_subdir = "VAL",
  errors_subdir  = "ERR",
  write_error_vectors = TRUE,
  class_raster = "D:/PROJECT/MASK/corine_groups.tif",
  class_band   = 1,
  class_lut    = "D:/PROJECT/MASK/corine_groups_lut.csv",
  write_classwise_metrics = TRUE,
  write_classwise_errors  = TRUE,
  write_confusion_vectors = TRUE,
  write_confusion_pixel_raster = TRUE,
  compute_det_overlap_stats = TRUE,
  digits_metrics = 3,
  digits_area    = 2,
  digits_res     = 0,
  write_xlsx = TRUE
)

# Multiple detection inputs (vector of paths); outputs are indexed by InputIndex
res_multi <- validate_fire_maps(
  input_shapefile = c("D:/PROJECT/DET/runA.gpkg", "D:/PROJECT/DET/runB.gpkg"),
  ref_shapefile   = "D:/PROJECT/REF/effis_ref.gpkg",
  mask_shapefile  = "D:/PROJECT/MASK/aoi_3035.shp",
  year_target     = 2025,
  validation_dir  = "D:/PROJECT/VALIDATION",
  metrics_type    = "all",
  template_res    = 90
)
}

}

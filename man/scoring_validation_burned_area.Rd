% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_scoring_burned_area_wrapper.R
\name{scoring_validation_burned_area}
\alias{scoring_validation_burned_area}
\title{Score validation between internal polygons and reference polygons}
\usage{
scoring_validation_burned_area(
  internal_polys,
  ref_polys,
  internal_keep_value = "keep",
  buffer_m = 0,
  save_outputs = FALSE,
  out_dir = NULL,
  prefix = "BA",
  driver = "GPKG",
  overwrite = TRUE,
  quiet = TRUE,
  save_splits = FALSE,
  suppress_sf_warnings = TRUE
)
}
\arguments{
\item{internal_polys}{sf. Internal polygons (must contain column `flag_internal`).}

\item{ref_polys}{sf. Reference polygons (e.g., EFFIS).}

\item{internal_keep_value}{Character vector. Values in `flag_internal` treated as "keep".}

\item{buffer_m}{numeric. Tolerance buffer (meters) used for both directions.}

\item{save_outputs}{logical. Save outputs to disk.}

\item{out_dir}{character. Output directory when saving.}

\item{prefix}{character. Prefix for filenames.}

\item{driver}{character. "GPKG" or "ESRI Shapefile".}

\item{overwrite}{logical.}

\item{quiet}{logical.}

\item{save_splits}{logical. If TRUE, save keep/ref splits too.}

\item{suppress_sf_warnings}{logical. Suppress sf warnings.}
}
\value{
list with `ref_flagged`, `internal_flagged`, and `out_paths` (if saved).
}
\description{
Produces two complementary products:
1) reference polygons flagged as keep_ref/review_ref against internal keep
2) internal keep polygons flagged as keep_common/review_int_no_effis against reference
}

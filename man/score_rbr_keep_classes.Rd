% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5_scoring_burned_area_rbr.R
\name{score_rbr_keep_classes}
\alias{score_rbr_keep_classes}
\title{Phase 2 scoring: RBR-based keep-like flagging and optional class-3 labeling}
\usage{
score_rbr_keep_classes(
  polys_sf,
  rbr_rast,
  out_dir,
  prefix = "phase2",
  keep_pool = NULL,
  use_keep_common_pool = TRUE,
  keep_common_col = "flag_internal_effis",
  keep_common_value = "keep_common",
  keep_fallback_col = "flag_internal",
  keep_fallback_value = "keep",
  max_keep_samples = 5e+06,
  above_keep_prob = 0.05,
  min_area_ha = 10,
  min_pix = NULL,
  promote_percentile = 0.1,
  promote_p_above_ref = 0.7,
  do_class3 = TRUE,
  out_col = "class3_id",
  good_percentile = 0.05,
  bad_percentile = 0.01,
  require_conf_ok = TRUE,
  chunk_size = 500,
  extract_exact = FALSE,
  verbose = TRUE,
  save_outputs = TRUE,
  driver = "GPKG",
  overwrite = TRUE,
  quiet = TRUE,
  save_splits = FALSE,
  arcgis_fix = TRUE,
  output_epsg = 3035,
  normalize_corine_field = TRUE,
  corine_na_col = "corine_na_frac",
  corine_alt_cols = c("NA_CORINE_fraction", "na_corine_fraction", "corine_na_fraction"),
  fill_na_corine = FALSE,
  fill_na_corine_value = 0
)
}
\arguments{
\item{polys_sf}{\code{sf} polygon layer to score (Phase-2 target polygons).}

\item{rbr_rast}{\code{terra::SpatRaster}. Single-band RBR raster (first layer is used).}

\item{out_dir}{Character. Output folder (created if missing).}

\item{prefix}{Character. Prefix used for output filenames (default: \code{"phase2"}).}

\item{keep_pool}{Optional list. If provided, the keep pool is reused and not recomputed.
Must contain: \code{min_area_ha}, \code{min_pix}, \code{promote_percentile},
\code{promote_p_above_ref}, \code{qref_prob}, \code{qref_keep}, \code{q25_keep},
\code{pixel_area_ha}, \code{keep_medians}.}

\item{use_keep_common_pool}{Logical. If TRUE, try to build the keep pool from
\code{keep_common_col == keep_common_value} first.}

\item{keep_common_col}{Character. Column used to define the common keep pool.}

\item{keep_common_value}{Character. Value indicating membership in the common keep pool.}

\item{keep_fallback_col}{Character. Fallback column used if the common pool is empty.}

\item{keep_fallback_value}{Character. Fallback value indicating keep membership.}

\item{max_keep_samples}{Numeric. Max number of keep-pool pixels to retain for quantiles.}

\item{above_keep_prob}{Numeric in (0,1). Quantile probability used to define \code{qref_keep}.}

\item{min_area_ha}{Numeric. Minimum polygon area (ha) required for confident scoring.}

\item{min_pix}{Integer or NULL. Minimum number of pixels required; if NULL it is derived
from \code{min_area_ha} and raster resolution (minimum 13 pixels).}

\item{promote_percentile}{Numeric in (0,1). Minimum percentile (within keep distribution)
required to classify a polygon as \code{keep_like}.}

\item{promote_p_above_ref}{Numeric in (0,1). Minimum proportion of pixels above \code{qref_keep}
required to classify a polygon as \code{keep_like}.}

\item{do_class3}{Logical. If TRUE, create a 3-level class column (\code{out_col}).}

\item{out_col}{Character. Output column name for class-3 labeling (default: \code{"class3_id"}).}

\item{good_percentile}{Numeric in (0,1). Percentile threshold above which polygons are labeled
\code{"good_identified"}.}

\item{bad_percentile}{Numeric in (0,1). Percentile threshold below which polygons are labeled
\code{"bad_identified"}.}

\item{require_conf_ok}{Logical. If TRUE, polygons failing area/pixel confidence are forced to
\code{"ambiguous"}.}

\item{chunk_size}{Integer. Chunk size for iterating polygons during raster extraction.}

\item{extract_exact}{Logical. Kept for API compatibility (not used; extraction uses
\code{terra::extract()} long-form output).}

\item{verbose}{Logical. If TRUE, prints progress messages.}

\item{save_outputs}{Logical. If TRUE, writes outputs to disk.}

\item{driver}{Character. \code{"GPKG"} or \code{"ESRI Shapefile"}.}

\item{overwrite}{Logical. If TRUE, overwrite existing outputs.}

\item{quiet}{Logical. Passed to \code{sf::st_write()}.}

\item{save_splits}{Logical. If TRUE, also saves \code{keep_only} and \code{review_only} layers.}

\item{arcgis_fix}{Logical. If TRUE, normalizes geometries and CRS for ArcGIS-friendly outputs.}

\item{output_epsg}{Integer. EPSG code used when \code{arcgis_fix = TRUE} (default 3035).}
}
\value{
A list with:
\itemize{
  \item \code{scored_sf}: input polygons with scoring fields appended.
  \item \code{keep_pool}: list describing the keep pool (can be reused).
  \item \code{out_paths}: list with output paths (if \code{save_outputs = TRUE}).
}
}
\description{
Computes per-polygon RBR summary statistics from an RBR raster and scores each polygon
relative to a "keep" reference pool (typically produced in Phase 1). The function:
(i) builds or reuses a keep-pool distribution, (ii) extracts per-polygon pixel values
from \code{rbr_rast}, (iii) computes robust features (median, pixel count, exceedance
probabilities), (iv) assigns a binary \code{keep_like} decision, and (optionally)
(v) assigns a 3-level quality class (\code{class3_id}) based on percentile thresholds
within the keep distribution.
}
\details{
The keep pool is selected from \code{polys_sf} using either a common pool
(\code{keep_common_col == keep_common_value}) or a fallback pool
(\code{keep_fallback_col == keep_fallback_value}). You can also supply \code{keep_pool}
to reuse a previously computed pool and avoid recomputation.

Outputs can be written to a GeoPackage (\code{driver = "GPKG"}) or Shapefile
(\code{driver = "ESRI Shapefile"}). When \code{arcgis_fix = TRUE}, the object written
is normalized to improve ArcGIS compatibility (valid geometries, MULTIPOLYGON, Z/M dropped,
optional reprojection to \code{output_epsg}).


Per-polygon fields added include:
\describe{
  \item{\code{n_pix}}{Number of extracted pixels.}
  \item{\code{median_rbr}}{Median RBR within the polygon.}
  \item{\code{p_above_keep_q25}}{Proportion of pixels above keep-pool Q25.}
  \item{\code{p_above_keep_ref}}{Proportion of pixels above \code{qref_keep}.}
  \item{\code{percentile_in_keep}}{ECDF percentile of \code{median_rbr} within keep medians.}
  \item{\code{keep_like}}{0/1 decision.}
  \item{\code{flag_rbr}}{\code{"keep"} or \code{"review"} based on \code{keep_like}.}
  \item{\code{class3_id}}{Optional: \code{"good_identified"}, \code{"bad_identified"}, \code{"ambiguous"}.}
}
}
\examples{
\dontrun{
# After Phase 1:
p1 <- scoring_burned_area_stage2(..., save_outputs = FALSE)

out_growth <- file.path(tempdir(), "phase2_demo")
dir.create(out_growth, recursive = TRUE, showWarnings = FALSE)

p2 <- score_rbr_keep_like_class3(
  polys_sf  = p1$internal$stage2_keep_review_clean,
  rbr_rast  = rbr_rast,
  out_dir   = out_growth,
  prefix    = "BA_2012_phase2",
  save_outputs = TRUE,
  driver = "GPKG"
)
}

}
\seealso{
\code{\link{scoring_burned_area_stage2}}
}

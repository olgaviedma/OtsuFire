% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supervised-final-model.R
\name{train_final_burned_model}
\alias{train_final_burned_model}
\title{Train the final supervised burned-area model}
\usage{
train_final_burned_model(
  train_features,
  config,
  oof_agg = NULL,
  random_to_burned_ratio = NULL,
  otsu_unburned_to_burned_ratio = NULL,
  feature_whitelist_override = NULL,
  feature_weights = NULL,
  include_shape_features = NULL,
  sampling_seed = NULL,
  seed = NULL,
  val_frac = NULL,
  group_col = NULL,
  nrounds_max = NULL,
  early_stopping_rounds = NULL,
  impute_numeric = NULL,
  impute_factor_missing = NULL,
  source_weights = NULL,
  artifact_hard_source = NULL,
  total_weight_ratio = NULL,
  labelled_layer = "train_features",
  out_dir = NULL,
  canonical_oof_fingerprint = NULL,
  overwrite = TRUE,
  verbose = TRUE,
  .internal_resolved = FALSE
)
}
\arguments{
\item{train_features}{\code{sf} object, \code{data.frame}, or GeoPackage path
containing labelled training features. Must contain \code{class}, \code{source},
\code{neg_type}, and the required feature columns. When a path is supplied,
the layer specified by \code{labelled_layer} is read.}

\item{config}{Required object of class \code{otsufire_supervised_burned_config},
created with \code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}}. Supplies the resolved
model parameters, sampling settings, feature controls, random seeds, and
output locations.}

\item{oof_agg}{Character scalar or \code{NULL}. Path to the aggregated OOF CSV
returned by \code{\link[=run_oof_diagnostics]{run_oof_diagnostics()}}. Used to enrich the model summary
with OOF threshold information. Does not affect model fitting. Default:
\code{NULL}.}

\item{random_to_burned_ratio}{Training control: cap on background
negatives as a multiple of the burned-label count. Builder setting:
\code{negative_pool_params$caps["random"]}. See \strong{Training and sampling
controls}.}

\item{otsu_unburned_to_burned_ratio}{Training control: cap on Otsu-based
negatives as a multiple of the burned-label count. Builder setting:
\code{negative_pool_params$caps["otsu"]}.}

\item{feature_whitelist_override}{Character vector restricting the
permitted feature set, or \code{NULL} to use the configured setting.
Deprecated as a function-level control; configure it in the builder.}

\item{feature_weights}{Named numeric vector of per-feature weights, or
\code{NULL} to use the configured setting. Deprecated as a function-level
control; configure it in the builder.}

\item{include_shape_features}{Logical scalar or \code{NULL}. Whether shape and
size features are eligible for modelling. When \code{NULL}, uses
\code{config$train_control$include_shape_features}. Required features must
have been prepared during extraction.}

\item{sampling_seed}{Training control: random seed for negative-pool
sampling. Builder argument: \code{final_sampling_seed}.}

\item{seed}{Training control: random seed for the inner split and XGBoost
training. Builder argument: \code{final_seed}.}

\item{val_frac}{Training control: inner validation fraction, between \code{0}
and \code{1}, excluding the endpoints. Builder argument: \code{val_frac}.}

\item{group_col}{Training control: column identifying observations kept
together in the inner train/validation split. Builder argument:
\code{group_col}.}

\item{nrounds_max}{Training control: maximum number of boosting rounds.
Builder argument: \code{nrounds_max}.}

\item{early_stopping_rounds}{Training control: early-stopping patience.
Builder argument: \code{early_stop}.}

\item{impute_numeric}{Training control: numeric imputation rule,
\code{"median"} or \code{"zero"}. Builder argument: \code{impute_numeric}.}

\item{impute_factor_missing}{Training control: category used for missing
factor values. Builder argument: \code{impute_factor_missing}.}

\item{source_weights}{Optional named numeric vector assigning row weights
by values of the \code{source} column, for example \code{c(artifact_hard = 0.3)}.
When \code{NULL}, reads \code{config$negative_pool_params$source_weights} if
available.}

\item{artifact_hard_source}{Character vector identifying source values for
promoted hard negatives, or \code{NULL}. When \code{NULL}, resolves to
\code{"artifact_hard"} if the mechanism is enabled, otherwise \code{character(0)}.}

\item{total_weight_ratio}{Numeric scalar or \code{NULL}. Target ratio of total
hard-negative weight to total burned-pool weight. When \code{NULL}, uses
\code{config$negative_pool_params$artifact_hard$total_weight_ratio}. Ignored
when an explicit hard-negative source weight is supplied through
\code{source_weights}. With no source-weight override and no hard-negative
rows, no explicit sample-weight vector is constructed.}

\item{labelled_layer}{Character scalar. Layer read when \code{train_features} is
a GeoPackage path. Default: \code{"train_features"}.}

\item{out_dir}{Character scalar or \code{NULL}. Output directory. Defaults to
\code{config$output_routes$final_model_dir}, normally the run's
\verb{07_FINAL_MODEL_V2} folder.}

\item{canonical_oof_fingerprint}{Internal argument used by the full
pipeline to compare the final feature schema with the OOF schema. A
mismatch raises an error. Leave as \code{NULL} for standalone use, which
computes and saves its own fingerprint.}

\item{overwrite}{Logical scalar. Controls replacement of the
training-selection GeoPackage. Default: \code{TRUE}. This argument is not
documented as a general reuse switch for all model files.}

\item{verbose}{Logical scalar. Whether to display progress messages.
Default: \code{TRUE}.}

\item{.internal_resolved}{Internal argument indicating that compatibility
settings have already been resolved by the pipeline. Leave as \code{FALSE} for
direct calls.}
}
\value{
A named list containing fitted objects, training records, and
output paths.

\strong{Returned objects}\tabular{ll}{
   Field \tab Contents \cr
   \code{model} \tab Fitted XGBoost model from the final refit. \cr
   \code{recipe} \tab Preprocessing and feature specification associated with the fitted model. \cr
   \code{training_ok} \tab Selected eligible training polygons as an \code{sf} object. \cr
   \code{split_idx} \tab Indices defining the inner train/validation split used for round selection. \cr
}


\strong{Output paths}\tabular{ll}{
   Field \tab Contents \cr
   \code{final_model_rds} \tab Path to the saved final model. \cr
   \code{recipe_rds} \tab Path to the saved recipe. \cr
   \code{feature_importance_csv} \tab Path to the feature-importance table. \cr
   \code{model_summary_txt} \tab Path to the model summary, including OOF information when supplied. \cr
   \code{meta_txt} \tab Path to the model metadata file. \cr
   \code{split_idx_rds} \tab Path to the saved split indices. \cr
   \code{training_ok_csv} \tab Path to the selected training table. \cr
   \code{training_ok_gpkg} \tab Path to the selected training polygons. \cr
}


Outputs are written under \code{out_dir}, normally \verb{07_FINAL_MODEL_V2}. Use the
returned paths to locate the files.
}
\description{
Train the final XGBoost burned-area model using the eligible labelled
examples selected under the configured sampling and weighting rules.

The function selects the number of boosting rounds using an inner
validation split, then refits the model on the complete selected training
set. It saves the fitted model, preprocessing recipe, training records, and
diagnostic summaries.

Run this stage after feature extraction and out-of-fold diagnostics. The
returned \code{model} and \code{recipe} are used by \code{\link[=score_supervised_burned_map]{score_supervised_burned_map()}}
to score candidate polygons.
}
\section{Training and sampling controls}{

The arguments \code{random_to_burned_ratio}, \code{otsu_unburned_to_burned_ratio},
\code{sampling_seed}, \code{seed}, \code{val_frac}, \code{group_col}, \code{nrounds_max},
\code{early_stopping_rounds}, \code{impute_numeric} and \code{impute_factor_missing}
default to \code{NULL}, meaning that the resolved configuration value is used.
They are retained for compatibility; use the corresponding builder
settings for new workflows.
}

\section{Model-fitting procedure}{

The function uses the following procedure:
\enumerate{
\item Identify eligible burned and unburned training examples.
\item Apply the configured negative-pool caps and sampling rules.
\item Resolve the active features and training weights.
\item Create an inner train/validation split, respecting the configured
grouping column.
\item Use early stopping to select the number of boosting rounds.
\item Refit the preprocessing recipe and a fresh model on all selected
eligible training examples.
\item Save the refitted model, recipe, training records, and summaries.
}

The saved model is the full selected-set refit, not the intermediate model
used for early stopping.

"All selected training examples" refers to the observations remaining
after eligibility checks and sampling. It does not necessarily include
every row supplied in \code{train_features}.
}

\section{Training labels and negative pools}{

Training requires explicit burned or unburned labels.

Positive labels originate from the labelled burned pool, including
Otsu-guided patches classified as \code{"keep"} during label preparation.
Review candidates and rows with missing or unknown labels do not
automatically become negatives.

Eligible negative sources include:
\itemize{
\item background negatives, identified as \code{random};
\item moderately burned-like negatives, identified as \code{otsu};
\item promoted strongly burned-like negatives, identified through
\code{artifact_hard_source}, when enabled.
}

Background and Otsu negatives are subject to their configured caps.
Hard-negative contribution is controlled through the applicable weighting
rules.
}

\section{Weighting}{

Predictor weights and training-example weights serve different purposes.\tabular{ll}{
   Setting \tab Applies to \cr
   \code{feature_weights} \tab Predictor columns. \cr
   \code{source_weights} \tab Training rows belonging to named sources. \cr
   \code{total_weight_ratio} \tab Total hard-negative contribution relative to the burned pool. \cr
}


Under pool-ratio weighting:
\preformatted{
total hard-negative weight =
  total_weight_ratio x total burned-pool weight
}

An explicit hard-negative weight in \code{source_weights} takes precedence over
this ratio. For example, a source weight of \code{0.3} is a per-row setting and
does not imply that the hard-negative pool has 30\% of the burned pool's
total weight.

Use consistent sampling and weighting settings for OOF and final training.
}

\section{Relationship to OOF diagnostics}{

The final model and OOF models use the same training procedure and shared
configuration.

They are fitted on different training subsets, so their preprocessing
statistics, class-balance weights, selected boosting rounds, and
predictions may differ.

The optional \code{oof_agg} file adds OOF threshold information to the model
summary. It does not change the selected training rows, fit a model,
calibrate scores, or alter the final fitted model.

OOF diagnostics assess agreement with internal labels. Independent map
validation is still needed to assess burned-area mapping accuracy.
}

\section{Feature recipe}{

The returned recipe records the transformations needed for prediction,
including imputation information, feature names and order, and applied
feature controls.

Keep the model and its recipe together. Use \code{\link[=score_supervised_burned_map]{score_supervised_burned_map()}}
to apply the saved transformations consistently to candidate features.

When shape features are enabled, the permitted feature set includes
\code{area_ha}, \code{n_pix}, \code{log_area}, \code{perim_m}, \code{compactness} and
\code{elongation}. Associated missingness indicators may also be included.
}

\section{Feature-schema checks}{

When the full pipeline supplies \code{canonical_oof_fingerprint}, the function
checks that the final structural feature schema matches the OOF schema
before training and saving.

Standalone calls with \code{canonical_oof_fingerprint = NULL} create their own
fingerprint. They do not, through that argument alone, verify agreement
with a previous OOF run.
}

\section{Inner validation split}{

\code{split_idx} records the inner train/validation partition used to select
the boosting-round count.

The final refit subsequently uses both parts of the selected dataset. The
inner validation observations are therefore not an independent test set
for the saved final model.
}

\section{Compatibility arguments}{

Methodological settings should be supplied through
\code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}}.

The function-level caps, feature whitelist and weights, seeds, validation
fraction, grouping, boosting controls, and imputation arguments are
deprecated compatibility options.

For these arguments:
\itemize{
\item \code{NULL} uses the resolved configuration value;
\item an override of a package-default setting emits an
\code{otsufire_deprecated_param} warning;
\item an override that conflicts with an explicit builder setting raises an
error.
}
}

\examples{
\dontrun{
# Configure final training with the same settings used for OOF
config <- build_supervised_burned_config(
  internal_decisions = "data/internal_decisions_2022.gpkg",
  change_index = "data/RBR_2022.tif",
  delayed_change_index = "data/RBR_delayed_2022.tif",
  topo = "data/elevation_slope.tif",
  corine_raster = "data/land_cover_2022.tif",
  burnable_mask = "data/burnable_mask_2022.tif",
  target_year = 2022L,
  output_dir = "results",
  run_name = "RBR_2022",
  negative_pool_params = list(
    caps = c(random = 1.0, otsu = 1.0)
  ),
  nrounds_max = 4000L,
  early_stop = 80L,
  final_sampling_seed = 42L,
  final_seed = 42L,
  val_frac = 0.15,
  group_col = "block_id",
  impute_numeric = "median",
  impute_factor_missing = "MISSING"
)

# Train from existing feature and OOF outputs
# Replace these paths with the outputs from the preceding stages.
final <- train_final_burned_model(
  train_features = "path/to/features_geometry.gpkg",
  config = config,
  oof_agg = "path/to/2022_balanced_patch_oof_agg.csv"
)

# Inspect the fitted model and preprocessing recipe
final$model
final$recipe

# Inspect the examples selected for training
table(final$training_ok$class, useNA = "ifany")
table(final$training_ok$source, useNA = "ifany")

# Locate the saved model and recipe
final$final_model_rds
final$recipe_rds

# Inspect feature importance
importance <- read.csv(final$feature_importance_csv)
head(importance)

# The model and recipe are now available for use with
# score_supervised_burned_map().
}

}
\seealso{
\code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}},
\code{\link[=extract_supervised_features]{extract_supervised_features()}}, \code{\link[=run_oof_diagnostics]{run_oof_diagnostics()}},
\code{\link[=score_supervised_burned_map]{score_supervised_burned_map()}}, \code{\link[=validate_supervised_execution]{validate_supervised_execution()}},
\code{\link[=run_oneyear_supervised_pipeline]{run_oneyear_supervised_pipeline()}}.

Other workflow: 
\code{\link{build_burned_mapping_config}()},
\code{\link{build_supervised_burned_config}()},
\code{\link{build_supervised_training_pools}()},
\code{\link{change_index_mosaic}()},
\code{\link{extract_supervised_features}()},
\code{\link{make_spatial_folds}()},
\code{\link{promote_artifact_hard_negatives}()},
\code{\link{run_deterministic_pipeline}()},
\code{\link{run_oneyear_supervised_pipeline}()},
\code{\link{run_oof_diagnostics}()},
\code{\link{score_supervised_burned_map}()}
}
\concept{workflow}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_validation_statistics.R
\name{validation_statistics}
\alias{validation_statistics}
\title{Compare a numeric variable across polygon categories with Kruskal-Wallis, Dunn letters, and plots}
\usage{
validation_statistics(
  polys,
  gpkg_layer = NULL,
  x_var,
  y_mode = c("attribute", "area_ha", "raster", "overlay_percent"),
  y_var = NULL,
  y_name = "Y",
  raster_list = NULL,
  raster_stat = c("mean", "median"),
  raster_template = NULL,
  raster_panel_name = "Period",
  overlay = NULL,
  overlay_union = TRUE,
  overlay_percent_base = c("polys", "overlay"),
  panel_vars = NULL,
  x_levels = NULL,
  alpha = 0.05,
  p_adjust = "bonferroni",
  show_points = TRUE,
  facet_type = c("wrap", "grid"),
  facet_scales = c("fixed", "free_y"),
  out_dir = NULL,
  file_prefix = "compare_by_group",
  width = 12,
  height = 7,
  dpi = 300,
  log_area = FALSE,
  log_area_var = "Area_ha",
  log_area_base = 10,
  log_eps = 1e-09,
  y_transforms = NULL
)
}
\arguments{
\item{polys}{sf object OR path to a vector file (.gpkg/.shp/.geojson).}

\item{gpkg_layer}{Optional layer name if polys is a .gpkg. If NULL, auto-picks a layer.}

\item{x_var}{Character. Name of the categorical column to use on the X axis.}

\item{y_mode}{One of c("attribute","area_ha","raster","overlay_percent").}

\item{y_var}{Character. If y_mode="attribute", the name of a numeric column in polys.}

\item{y_name}{Character. Output column name for the numeric variable (default "Y").}

\item{raster_list}{Named list of raster paths (or SpatRaster). Only used if y_mode="raster".
Example: list(fire="...2012.tif", p1="...2013.tif", p2="...2014.tif")}

\item{raster_stat}{One of c("mean","median"). Statistic per polygon for raster extraction.}

\item{raster_template}{Optional SpatRaster used as template for alignment (res/crs).
If NULL and raster_list provided, the first raster becomes the template.}

\item{raster_panel_name}{Character. Name of the panel column created for raster_list names (default "Period").}

\item{overlay}{sf object OR path. Only used if y_mode="overlay_percent".}

\item{overlay_union}{Logical. If TRUE, union overlay geometries before intersection (avoids double counting).}

\item{overlay_percent_base}{One of c("polys","overlay"). If "polys": percent of each polygon covered by overlay.}

\item{panel_vars}{Character vector of columns used to define separate panels/tests (optional).
Examples: c("Source","Period") or c("RBR_year","Source"). If NULL, single global test.}

\item{x_levels}{Optional explicit order for x_var factor levels.}

\item{alpha}{Numeric. Significance level for deciding whether to compute Dunn letters (default 0.05).}

\item{p_adjust}{Character. P adjustment method for Dunn p-values (default "bonferroni").}

\item{show_points}{Logical. Add jittered points over boxplots (default TRUE).}

\item{facet_type}{One of c("wrap","grid"). If grid and panel_vars has 2 vars, uses facet_grid(row~col).}

\item{out_dir}{Optional directory to save outputs (plot + CSV summaries). If NULL, nothing is saved.}

\item{file_prefix}{Output prefix for saved files (default "compare_by_group").}

\item{width, height, dpi}{Plot saving parameters.}
}
\value{
A list with:
- data: long data.frame (all original attributes + new columns)
- kw: Kruskal-Wallis summary per panel
- letters: letter table per panel (if computed)
- plot: ggplot object
- paths: saved file paths (if out_dir provided)
}
\description{
Given polygons (sf or path), this function builds a long data.frame where:
- X is a categorical column (e.g., Confusion, flag_ref, flag_internal)
- Y is a numeric variable computed from:
  - an existing attribute column (y_mode="attribute")
  - polygon area in hectares (y_mode="area_ha")
  - extraction from one or more rasters (y_mode="raster") (e.g., RBR fire/p1/p2)
  - percent overlap with an overlay layer (y_mode="overlay_percent") (e.g., % regenerated)

It runs Kruskal-Wallis per panel (optional), then Dunn post-hoc (PMCMRplus) with letters
(multcompView), and produces a ggplot (boxplot + optional jitter) with letters.
}

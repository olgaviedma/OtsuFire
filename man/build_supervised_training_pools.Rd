% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supervised-pools.R
\name{build_supervised_training_pools}
\alias{build_supervised_training_pools}
\title{Build training and scoring pools for supervised burned-area mapping}
\usage{
build_supervised_training_pools(
  config,
  deterministic_decisions = NULL,
  write_outputs = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{config}{Required object of class \code{otsufire_supervised_burned_config},
created with \code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}}. Supplies the inputs,
negative-pool settings, random seeds, tool paths, and output locations.}

\item{deterministic_decisions}{Optional \code{sf} polygon object, polygon
\code{terra::SpatVector}, or GeoPackage path containing the decision layer for
Otsu-guided patches. For a GeoPackage, the expected layer is
\code{internal_decisions}. When \code{NULL}, uses \code{config$inputs$internal_decisions}.}

\item{write_outputs}{Logical scalar. Whether to write the pool GeoPackage
and quality-assurance files. Default: \code{TRUE}.}

\item{overwrite}{Logical scalar. Passed to the unburned-pool builder to
control replacement of existing outputs. Default: \code{FALSE}.}
}
\value{
A named list containing pool objects, output paths, and audit
information.

\strong{Pool objects}\tabular{ll}{
   Field \tab Contents \cr
   \code{burned_pool} \tab Burned training pool as an \code{sf} object. \cr
   \code{unburned_pool} \tab Unburned training pool as an \code{sf} object. \cr
   \code{review_pool} \tab Review candidates as an \code{sf} object. \cr
   \code{scoring_pool} \tab Candidate scoring pool as an \code{sf} object. \cr
   \code{train_labeled} \tab Combined labelled training set containing burned and unburned examples. \cr
}


\strong{Output paths}\tabular{ll}{
   Field \tab Contents \cr
   \code{pools_gpkg} \tab Path to the pool GeoPackage used by subsequent stages. \cr
   \code{audited_internal_gpkg} \tab Path to the audited input decision layer. \cr
   \code{qa_summary_csv} \tab Path to the quality-assurance summary. \cr
   \code{qa_transitions_csv} \tab Path to the record of label transitions during quality checks. \cr
   \code{qa_reasons_csv} \tab Path to the quality-assurance reason summary. \cr
}


\strong{Auxiliary outputs and audits}\tabular{ll}{
   Field \tab Contents \cr
   \code{unburned_hard} \tab Auxiliary hard-negative output from unburned-pool construction. \cr
   \code{unburned_random} \tab Auxiliary random-background negative pool. \cr
   \code{unburned_final_raw} \tab Auxiliary combined unburned output before final pool assembly. \cr
   \code{exclusion_buffer} \tab Spatial exclusion buffer used during negative-pool construction. \cr
   \code{b4_audit} \tab Background-sampling audit, including cell counts, exclusions, percentile information, selected observations, and burnable-domain checks. \cr
   \code{neg_pool_fingerprint} \tab List with \code{text} and \code{checksum}, identifying the negative-pool inputs and settings. \cr
   \code{otsu_random_dedup_audit} \tab Record of random-background examples removed because they intersect Otsu negatives. \cr
}


\code{otsu_random_dedup_audit} includes \code{random_before},
\code{random_removed_by_otsu}, \code{random_final}, \code{area_removed} and
\code{removed_ids_hash}.

The auxiliary field \code{unburned_hard} should not be assumed to be identical
to the optional \code{artifact_hard} training bucket solely because of its name.
}
\description{
Build the burned, unburned, review, and scoring pools used by the
supervised OtsuFire workflow.

The function reads the decision layer for Otsu-guided patches, applies
quality checks to the initial labels, constructs negative examples, and
assembles the labelled training set.

Run this stage before spatial-fold assignment and feature extraction. The
returned \code{train_labeled} object contains the training examples, while the
written pool GeoPackage can be passed to subsequent workflow stages.

This function is also called as part of
\code{\link[=run_oneyear_supervised_pipeline]{run_oneyear_supervised_pipeline()}}.
}
\section{Workflow}{

The function performs the following steps:
\enumerate{
\item Read the decision layer for Otsu-guided patches.
\item Apply conservative quality-assurance checks and relabelling through
\code{audit_deterministic_pools()}.
\item Assemble the burned, review, and scoring pools.
\item Build negative examples from the decision-layer workflow, random
burnable-background samples, and Otsu residual patches.
\item Remove spatial duplication between background samples and Otsu
negatives.
\item Combine burned and unburned examples into \code{train_labeled}.
\item Write the pool GeoPackage and audit files when \code{write_outputs = TRUE}.
}

The initial labels are derived from Otsu-guided patch classifications.
They are not independent ground-truth observations.
}

\section{Pool roles}{
\tabular{ll}{
   Pool \tab Purpose \cr
   \code{burned_pool} \tab Positive training examples retained after quality checks. \cr
   \code{unburned_pool} \tab Negative training examples assembled from the supported unburned sources. \cr
   \code{review_pool} \tab Ambiguous candidates retained separately from the labelled training set. \cr
   \code{scoring_pool} \tab Candidate polygons prepared for subsequent supervised scoring. \cr
   \code{train_labeled} \tab Combined burned and unburned training examples. \cr
}


Review candidates are not automatically assigned unburned labels.
}

\section{Negative-pool construction}{

Negative-pool construction uses \code{build_unburned_from_deterministic_decisions()}
and \code{build_otsu_negative_pipeline()}.

The first builder uses classifications of Otsu-guided patches and random
burnable-background sampling. The second supplies current-year Otsu
residual negatives.

A patch classified as \code{"drop"} must satisfy the applicable selection rules
to enter the negative pool. Rejection from the burned pool alone does not
establish an unburned training label.

User-configurable negative-pool settings are supplied through
\code{config$negative_pool_params}, with random seeds and runtime controls
resolved by \code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}}.
}

\section{Burnable-background sampling}{

The background change-index percentile is calculated within the aligned
burnable domain. Eligible cells and random samples are selected from that
same domain.

The sampling audit records the domain, exclusions, percentile threshold,
eligible-cell counts, and selected observations. It also records whether
any selected observation falls outside burnable land.

Sampling uses the configured random seed. Reproducing a sample requires the
same inputs, settings, and sampling domain.
}

\section{Spatial deduplication}{

Otsu residual patches take priority over random-background examples.

Any random-background polygon that intersects an Otsu negative patch is
removed before capping and training-set assembly. This prevents the same
location from entering these two negative sources simultaneously.

The removal is recorded in \code{otsu_random_dedup_audit}.
}

\section{Sampling caps}{

Background and Otsu-negative caps are configured through
\code{config$negative_pool_params$caps}.

Each cap is expressed as a multiple of the burned-label count. The resolved
caps are also available in \code{config$train_control$caps}.

The Otsu negative builder passes its valid drop pool forward for
subsequent selection under this policy. Configure caps through
\code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}} rather than using legacy cap arguments.

The returned labelled pool should not be interpreted as the exact set of
observations used by every fitted model. Subsequent training stages apply
eligibility checks and sampling to their respective training subsets.
}

\section{Cache provenance}{

The function returns a negative-pool fingerprint containing a text
description and checksum.

The fingerprint records relevant inputs and settings, including the aligned
burnable mask, percentile configuration and value, random seeds, and
exclusions. A cached pool with a different fingerprint is invalidated
rather than reused.

Matching the target year alone is insufficient to establish that a cached
pool is compatible.
}

\section{Output files}{

When outputs are written, the pool GeoPackage uses the following path
relative to the configured run directory:
\verb{01_POOLS/<year>_<run_label>_pools.gpkg}.

The function also writes quality-assurance outputs and a fingerprint
sidecar: \verb{01_POOLS/<year>_<run_label>_neg_pool_fingerprint.txt}.

Use the returned paths to locate the products instead of reconstructing
the full directory structure.
}

\examples{
\dontrun{
# Configure the supervised workflow
config <- build_supervised_burned_config(
  run_label = "balanced",
  internal_decisions = "data/internal_decisions_2022.gpkg",
  change_index = "data/RBR_2022.tif",
  delayed_change_index = "data/RBR_delayed_2022.tif",
  topo = "data/elevation_slope.tif",
  corine_raster = "data/land_cover_2022.tif",
  burnable_mask = "data/burnable_mask_2022.tif",
  target_year = 2022L,
  output_dir = "results",
  run_name = "RBR_2022",
  random_seed = 42L,
  negative_pool_params = list(
    random = list(
      n_cells = 1500L,
      rbr_quantile = 0.50
    ),
    caps = c(
      random = 1.0,
      otsu = 1.0
    )
  )
)

# Build and write the pools
pools <- build_supervised_training_pools(
  config = config,
  write_outputs = TRUE,
  overwrite = FALSE
)

# Inspect pool sizes
c(
  burned = nrow(pools$burned_pool),
  unburned = nrow(pools$unburned_pool),
  review = nrow(pools$review_pool),
  scoring = nrow(pools$scoring_pool)
)

# Inspect sampling and deduplication audits
pools$b4_audit
pools$otsu_random_dedup_audit

# Inspect the negative-pool fingerprint
pools$neg_pool_fingerprint

# Locate the written pool GeoPackage
pools$pools_gpkg

# Continue with spatial-fold assignment
folds <- make_spatial_folds(
  pools$pools_gpkg,
  config = config
)
}

}
\seealso{
\code{\link[=build_supervised_burned_config]{build_supervised_burned_config()}}, \code{\link[=make_spatial_folds]{make_spatial_folds()}},
\code{\link[=extract_supervised_features]{extract_supervised_features()}}, \code{\link[=run_oneyear_supervised_pipeline]{run_oneyear_supervised_pipeline()}},
\code{audit_deterministic_pools()},
\code{build_unburned_from_deterministic_decisions()},
\code{build_otsu_negative_pipeline()}.

Other workflow: 
\code{\link{build_burned_mapping_config}()},
\code{\link{build_supervised_burned_config}()},
\code{\link{change_index_mosaic}()},
\code{\link{extract_supervised_features}()},
\code{\link{make_spatial_folds}()},
\code{\link{promote_artifact_hard_negatives}()},
\code{\link{run_deterministic_pipeline}()},
\code{\link{run_oneyear_supervised_pipeline}()},
\code{\link{run_oof_diagnostics}()},
\code{\link{score_supervised_burned_map}()},
\code{\link{train_final_burned_model}()}
}
\concept{workflow}

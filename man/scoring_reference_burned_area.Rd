% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_scoring_burned_area_wrapper.R
\name{scoring_reference_burned_area}
\alias{scoring_reference_burned_area}
\title{Score/prepare reference burned area polygons (e.g., EFFIS) with the same
per-polygon diagnostics used for internal polygons}
\usage{
scoring_reference_burned_area(
  ref_polys,
  burnable_corine,
  burnable_classes = NULL,
  rbr_rast = NULL,
  max_corine_na_frac = 0.7,
  na_exact = FALSE,
  burnable_exact = FALSE,
  clip_to_burnable = FALSE,
  clip_mask_buffer_m = 0,
  clip_min_piece_area_m2 = 0,
  clip_keep_largest_piece = FALSE,
  preyear_polys = NULL,
  erase_mask_buffer_m = 0,
  erase_post_shave_m = 0,
  erase_min_area_m2 = 0,
  compute_median = TRUE,
  median_colname = "median_rbr",
  median_chunk_size = 500L,
  save_outputs = FALSE,
  out_dir = NULL,
  prefix = "BA",
  driver = "GPKG",
  overwrite = TRUE,
  quiet = TRUE,
  save_splits = FALSE,
  touches = TRUE,
  suppress_sf_warnings = TRUE
)
}
\arguments{
\item{ref_polys}{sf. Reference polygons (e.g., EFFIS).}

\item{burnable_corine}{sf or terra::SpatRaster. Burnable mask used to check domain
and compute NA coverage when raster-based.}

\item{burnable_classes}{Integer vector or NULL. If `burnable_corine` is categorical,
these values are treated as burnable. If NULL, `burnable_corine` must be binary-like (0/1).}

\item{rbr_rast}{Optional terra::SpatRaster. If provided and `compute_median=TRUE`,
adds per-polygon median to `median_colname`.}

\item{max_corine_na_frac}{Numeric or NULL. If finite, polygons with NA fraction in
`burnable_corine` greater than this value are flagged as `drop_corine_na`.}

\item{na_exact}{Logical. If TRUE, uses exact extraction weights when computing NA fraction.}

\item{burnable_exact}{Logical. If TRUE, uses exact extraction weights for burnable hit test.}

\item{clip_to_burnable}{Logical. If TRUE, clip polygons to burnable pixels before scoring.
Requires `burnable_corine` as a raster.}

\item{clip_mask_buffer_m}{Numeric. Buffer (meters) for expanding burnable mask in raster clip.}

\item{clip_min_piece_area_m2}{Numeric. Minimum area (m2) for clipped pieces to keep.}

\item{clip_keep_largest_piece}{Logical. If TRUE, keep only the largest clipped piece per polygon.}

\item{preyear_polys}{Optional sf. If provided, overlaps are erased from kept polygons.}

\item{erase_mask_buffer_m}{Numeric. Buffer on erase mask (meters).}

\item{erase_post_shave_m}{Numeric. Post-erase shave (meters).}

\item{erase_min_area_m2}{Numeric. Minimum area (m2) to keep after erase.}

\item{compute_median}{Logical. If TRUE, compute median RBR for kept polygons.}

\item{median_colname}{Character. Column name for median RBR.}

\item{median_chunk_size}{Integer. Chunk size for median extraction.}

\item{save_outputs}{Logical. Save outputs to disk.}

\item{out_dir}{Character. Output directory when saving.}

\item{prefix}{Character. Prefix for filenames.}

\item{driver}{Character. "GPKG" or "ESRI Shapefile".}

\item{overwrite}{Logical.}

\item{quiet}{Logical.}

\item{save_splits}{Logical. If TRUE, save kept/dropped splits too.}

\item{touches}{Logical. Passed to terra::rasterize in clip-to-burnable.}

\item{suppress_sf_warnings}{Logical. Suppress common sf warnings.}
}
\value{
list with:
- ref_flagged: reference polygons with diagnostics and `flag_ref_internal`
- ref_flagged_clean: flagged layer where kept geometries are replaced by erased geometries (when available)
- ref_kept, ref_kept_clean, ref_dropped
- ref_kept_erased (NULL if no erase was performed)
- out_paths (paths when saved)
}
\description{
This helper is designed to make reference polygons directly comparable to the
internally detected polygons by producing:
- a burnable-domain quality flag (`flag_ref_internal`) with optional drops for
  high NA coverage in the burnable raster and/or non-burnable locations
- optional per-polygon median RBR (`median_colname`)
- optional pre-year erase (same geometry cleaning logic as internal)
}
\details{
Unlike `scoring_internal_burned_area()`, this function does not use Stage-1 support
to define keep/review. Instead, it focuses on harmonizing per-polygon diagnostics
and optional domain filtering.
}

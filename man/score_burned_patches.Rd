% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deterministic-score.R
\name{score_burned_patches}
\alias{score_burned_patches}
\title{Score candidate burned patches and assign final decisions}
\usage{
score_burned_patches(
  burned_candidates,
  config,
  keep_pool = NULL,
  write_outputs = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{burned_candidates}{Candidate polygons produced by the detection and
refinement workflow. Accepts an \code{sf} polygon object, a polygon
\code{terra::SpatVector}, or a path to a readable vector file. The
\code{refined_patches_path} returned by \code{\link[=detect_burned_patches]{detect_burned_patches()}} can be
supplied directly.}

\item{config}{An object of class \code{otsufire_burned_mapping_config}, created
with \code{\link[=build_burned_mapping_config]{build_burned_mapping_config()}}. Contains the supporting inputs,
scoring settings, temporal-cleanup settings, and output locations.}

\item{keep_pool}{Optional spectral-support reference object compatible with
\code{\link[=score_rbr_keep_classes]{score_rbr_keep_classes()}}. Explicit pools must contain reference samples
from years other than the target year. When \code{NULL}, the function
constructs a local reference from the early high-confidence candidate
core for the target year. Default: \code{NULL}.}

\item{write_outputs}{Logical scalar. Whether to write scoring products to
disk. When \code{FALSE}, returned path fields may be \code{NA}. Default: \code{TRUE}.}

\item{overwrite}{Logical scalar. Whether existing output files may be
replaced. Default: \code{FALSE}.}
}
\value{
A named list containing scoring results, output paths, and
diagnostics.\tabular{ll}{
   Field \tab Description \cr
   \code{internal_decisions} \tab Main candidate decision layer, containing filter outcomes, support metrics, temporal diagnostics, decision reasons, and \code{class_final}. \cr
   \code{internal_decisions_path} \tab Path to the written candidate decision layer. \cr
   \code{reference_decisions} \tab Reference-side Otsu-guided segmentation decision layer, when available. \cr
   \code{reference_decisions_path} \tab Path to the written reference-side decision layer, when available. \cr
   \code{keep_pool_summary} \tab Summary of the spectral-support reference used during scoring. \cr
   \code{scoring_diagnostics} \tab Additional diagnostics and scoring metadata. \cr
}


Path fields may be \code{NA} when the corresponding products are not written.
The reference-side decision outputs do not constitute external map
validation.
}
\description{
Evaluate candidate burned patches using the Otsu-guided segmentation scoring
rules and assign each patch a final decision: \code{"keep"}, \code{"review"}, or
\code{"drop"}.

The function combines seed support, burnable-land context, optional
active-fire hotspot evidence, temporal assessment, and spectral support.
Filter outcomes, supporting metrics, and decision reasons are retained in
the output layer.

Use this function after \code{\link[=detect_burned_patches]{detect_burned_patches()}}. Validation against an
external burned-area map is performed separately with
\code{\link[=validate_fire_maps]{validate_fire_maps()}}.
}
\section{Scoring workflow}{

Candidates are evaluated through three filters and a separate temporal
assessment.\tabular{lll}{
   Component \tab Evidence assessed \tab Output fields \cr
   Filter 1 \tab Seed support, burnable-land context, vegetation availability, and non-burnable contamination. \tab \code{filter_1}, \code{reason_1} \cr
   Filter 2 \tab Corroboration from active-fire hotspots, when available. \tab \code{filter_2}, \code{reason_2} \cr
   Temporal assessment \tab Overlap and conflicts with previous-year burned areas. \tab \code{preyear_overlap_frac}, \code{preyear_action}, \code{preyear_reason} \cr
   Filter 3 \tab Spectral support relative to a high-confidence reference distribution, together with minimum area and pixel support. \tab \code{filter_3}, \code{reason_3} \cr
}


Final decisions are stored in \code{class_final}. Individual filter outcomes
represent intermediate assessments.
}

\section{Filter 1 (seed support and burnable-land context)}{

Filter 1 assesses internal seed support and the plausibility of the
candidate's land-cover context.

Seed-supported candidates remain on the keep pathway. Candidates without
seed support are assessed against the burnable domain:
\itemize{
\item burnable but unsupported candidates are assigned to review;
\item non-burnable candidates are assigned to drop;
\item candidates with excessive missing vegetation information may also be
dropped.
}

This filter is independent of the spectral-support reference.
}

\section{Filter 2 (hotspot corroboration)}{

When hotspot observations are available for the target year:
\itemize{
\item hotspot-supported candidates receive \code{filter_2 = "keep"};
\item candidates without hotspot support receive \code{filter_2 = "review"}.
}

The absence of hotspot support does not, by itself, produce a drop
decision in this filter.

When hotspot data are unavailable, Filter 2 is recorded as not applied.
}

\section{Temporal assessment}{

When a previous-year burned-area map is supplied, overlap is assessed to
identify potential residual scars, repeated detections, and temporal
conflicts.

Overlap fractions, temporal actions, and their reasons are retained
separately from the numbered filters.
}

\section{Filter 3 (spectral support)}{

Filter 3 compares candidate spectral responses with a high-confidence
reference distribution.

The following metrics are recorded:\tabular{ll}{
   Field \tab Description \cr
   \code{percentile_in_keep} \tab Percentile position within the spectral-support reference distribution. \cr
   \code{p_above_keep_ref} \tab Fraction of pixels exceeding the reference support threshold. \cr
   \code{p_above_keep_q25} \tab Fraction of pixels exceeding the lower reference support threshold. \cr
   \code{area_ha} \tab Patch area in hectares. \cr
   \code{n_pix} \tab Number of valid change-index pixels. \cr
   \code{conf_area} \tab Flag indicating whether minimum area requirements are met. \cr
   \code{conf_pix} \tab Flag indicating whether minimum pixel-support requirements are met. \cr
}


Candidates receive \code{filter_3 = "keep"} when they meet the percentile and
exceedance criteria and satisfy the minimum area and pixel-support
requirements.

Borderline candidates generally receive \code{filter_3 = "review"}. Drop
outcomes are mainly associated with insufficient geometric support, such
as \code{conf_area = "too_small"} or \code{conf_pix = "too_few_pix"}.
}

\section{Spectral-support references}{

Two reference modes are supported.

\strong{Local reference from the target year.} Use \code{keep_pool = NULL}, the
default.

The function constructs a reference from the early, seed-supported
candidate core: polygons entering the intermediate keep/review layer with
\code{flag_internal = "keep"}.

This reference is constructed before final confidence assignment. It is
not selected from the final \code{class_final = "keep"} output and does not use
an external burned-area registry or validation map.

The reference provides the thresholds and empirical distribution used by
Filter 3, including \code{qref_keep}, \code{q25_keep}, and \code{keep_medians}.

\strong{Explicit reference from other years.} Supply a reference object
prepared from trusted decision layers for years other than
\code{config$target_year}.

Use \code{\link[=collect_keep_reference_samples]{collect_keep_reference_samples()}} and \code{\link[=build_keep_pool_from_samples]{build_keep_pool_from_samples()}}
to prepare reference samples and summaries. The supplied object must match
the structure expected by \code{\link[=score_rbr_keep_classes]{score_rbr_keep_classes()}}.

When an explicit pool is supplied, Filter 3 uses it directly instead of
constructing the local reference.

The standard helper workflow selects reference samples meeting all of the
following conditions:
\itemize{
\item \code{class_final == "keep"};
\item \code{filter_1 == "keep"};
\item \code{filter_2 == "keep"};
\item \code{filter_3 == "keep"};
\item \code{conf_area == "ok"};
\item \code{conf_pix == "ok"};
\item compliance with the minimum-area requirement;
\item no temporal drop when \code{preyear_action} is available.
}

These selection rules differ from those used for the local reference.

Explicit pools containing the target year are not supported and are
rejected when helper-generated metadata identify that overlap. Ensure that
the target year is excluded when preparing an explicit pool.

Legacy objects of the form \code{list(keep_pool = summary)} are also accepted.
}

\section{Final decisions}{

Filter outcomes are integrated into the \code{class_final} field.\tabular{ll}{
   Value \tab Interpretation \cr
   \code{"keep"} \tab High-confidence burned candidate under the rule-based filters. \cr
   \code{"review"} \tab Ambiguous candidate or candidate with incomplete or conflicting support. \cr
   \code{"drop"} \tab Candidate rejected by the rule-based filters. \cr
}


If exactly one active filter assigns drop and all remaining active filters
assign keep, the final decision is softened to review.

These classes are rule-based decisions, not estimated probabilities of
burning. Assess map accuracy separately using suitable reference data.
}

\examples{
\dontrun{
# Configure an annual RBR workflow
config <- build_burned_mapping_config(
  change_index = "data/RBR_2022.tif",
  vegetation_map = "data/vegetation_classes.tif",
  burnable_mask = "data/burnable_mask.tif",
  hotspots = "data/hotspots_2022.gpkg",
  previous_year_burned = "data/burned_2021.gpkg",
  target_year = 2022L,
  output_dir = "results",
  run_name = "RBR_2022"
)

# Detect and refine candidate patches
detection <- detect_burned_patches(
  config = config,
  write_outputs = TRUE
)

# Score candidates using the local target-year reference
scored <- score_burned_patches(
  burned_candidates = detection$refined_patches_path,
  config = config,
  keep_pool = NULL,
  write_outputs = TRUE,
  overwrite = FALSE
)

# Summarise final decisions
table(
  scored$internal_decisions$class_final,
  useNA = "ifany"
)

# Inspect diagnostics and the decision-layer path
scored$scoring_diagnostics
scored$internal_decisions_path

# Use a previously prepared reference from other years
# The saved object must have the required keep-pool structure
# and must exclude the target year, 2022.
historical_pool <- readRDS("data/keep_pool_other_years.rds")

scored_external <- score_burned_patches(
  burned_candidates = detection$refined_patches_path,
  config = config,
  keep_pool = historical_pool,
  write_outputs = FALSE
)

table(
  scored_external$internal_decisions$class_final,
  useNA = "ifany"
)
}

}
\seealso{
\code{\link[=build_burned_mapping_config]{build_burned_mapping_config()}}, \code{\link[=detect_burned_patches]{detect_burned_patches()}},
\code{\link[=run_deterministic_pipeline]{run_deterministic_pipeline()}}, \code{\link[=collect_keep_reference_samples]{collect_keep_reference_samples()}},
\code{\link[=build_keep_pool_from_samples]{build_keep_pool_from_samples()}}, \code{\link[=score_rbr_keep_classes]{score_rbr_keep_classes()}},
\code{\link[=validate_fire_maps]{validate_fire_maps()}}.
}
